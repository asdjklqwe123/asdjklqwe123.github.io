---
title: '操作系统复习'
description: '操作系统核心知识点：中断机制、CPU特权级、程序结构与内存管理'
publishDate: 2025-12-18
category: course
tags: ['操作系统']
---

系统就是抽象、协调和权衡。计算机操作系统建立起对资源的分层抽象，并通过权衡利弊，统筹、协调和管理资源来达到最大化计算机针对特定用途的效用。


---

## 设备与中断

前瞻
1. ISR 是什么
2. CPU 怎么处理中断，流程是什么，IP 变化是什么
3. 中断有哪些种类
4. 为何要给 INT3 分配特殊的单字节编码?


**PPT2**
- 中断服务程序	ISR
	- 由CPU在响应中断时，自动调用的一段对应于中断号的特殊过程。
- 当中断来临时，CPU**执行完**当前正在执行的指令，就跳转去ISR，且IRET返回时返回到**下一条指令处**。
	- 特例1：REP字符串指令，拷贝完一次后就跳转，回来接着拷贝
	- 特例2：如果中断是一个异常，则IRET返回到指令本身**再次尝试执行**
	- 特例3：**STI、IRET指令以及操作段寄存器的指令**会导致执行完本指令之后再执行指令才响应中断。


- 外部中断
- 异常
	- 当程序中的指令执行发生错误，就会抛出异常，异常会被同步捕获
	- 会被同步捕获（和通常的中断与CPU异步不同）
- 陷阱
	- 将断点位置换成INT 3
	- 系统调用是陷阱的一种特殊情况



--- 

## 处理器特权级

**硬件特权级**	CPU硬件实现，分为用户模式（用户态）和内核模式（内核态）。

**用户模式**	执行应用程序的模式，不允许直接访问系统的敏感资源。
**内核模式**	执行操作系统的模式，可直接执行敏感指令（**特权指令？**）和访问敏感资源。

**特权指令**	能对系统中敏感资源进行操作的指令，仅能在内核模式下执行。

特权级的切换：
- 方法一：中断机制
- 方法二：专用指令机制
用户程序使用什么方法调用操作系统：
- 使用上述两种机制之一。
- 之后陷入操作系统内核，执行被操作系统接管。

--- 

## 简单程序的结构 PPT3.1

**前瞻**
1. 运行时视图是什么
2. 程序有哪些段，分别是什么，有什么用
3. 程序在外存中是怎么存储的
4. 操作系统如何将程序从外存搬到 RAM 中

**运行时视图**
- 程序在**执行时**的主存储器布局。也叫运行时布局
**代码段**
- 存放程序的可执行指令，所有的执行都在代码段发生。
**数据段**
- 存放程序的数据
- 只读数据段：存放程序的含初值常量，程序运行时不可修改
- 读写数据段：存放程序的含初值常量，程序运行时可修改
- 零初始化数据段：存放程序的不含初值可修改常量
**堆段**
- 存放程序的堆，也即动态分配内存时内存的来源。
**栈段**
- 存放程序的运行栈，以供过程调用时保存和恢复上下文。

### **程序的装入**
操作系统读取外存上的可执行文件中对程序**逻辑布局**的描述（Description），在内存中生成程序的**物理布局**的一个实例（Instance）的过程。
（通常对栈段不做处理）

### **程序的生成**
**链接器**的两个方法：
1. 直接回填法：
	1. 链接器生成所有符号的地址后，直接修改 .text 段中对这些符号的引用，将正确的地址填写到那些引用中去，这样生成的代码就可以访问那些符号了。
	2. **会修改.text段**。！！
2. 间接地址法：
	1. 编译器或汇编器生成目标文件时，在.rwdata段留出一个表格，.text段中的代码对其它符号的引用均通过这个表格进行。
	2. **不会修改.text段，但会修改.rwdata段。**

**调用约定**：
- **传值约定**与**保存约定**合起来称为调用约定。
- 传参约定：3种
- 保存约定：
	- 寄存器保存约定：
		- 调用者负责
			- 优点：可以少保护寄存器。
			- 缺点：需要明确知道被调用者会触碰哪些寄存器，以及自己的哪些寄存器里面有有效数据，才能针对性地生成最小的保护列表。对人而言不那么容易，而且保护列表要反复写，增加代码量。
		- 被调用者负责
			- 优点：容易做到权责一致、做到模块化，且保护列表只用写一次。
			- 缺点：产生大量不必要的PUSH和POP，拖慢程序执行速度
		- 混合制负责
			- 缺点：约定复杂，写程序时要准确记得哪些寄存器是谁负责保存，一旦弄错后果就很严重。
	- 栈框恢复约定
		- 调用者负责
			- 我们总是先保存SP到BP，然后结束调用后将BP赋给SP。
		- 调用者负责、被调用者负责、混合制负责

**过程执行的全景**（STDCALL）
1. 序言	Prologue：
	- 真正的运算操作开始之前所做的准备工作
	- CALL指令、保存寄存器、读取参数、初始化局部变量

2. 尾声	Epilogue：
	  - 真正的运算操作结束之后所做的收尾工作，
	  - 恢复寄存器、调整栈框、RET指令

3. 开销：序言和尾声之和。

---

## 复杂程序的结构 PPT3.2
前瞻：
1. 运行时环境在哪个模式执行
2. 多程序地址空间冲突怎么解决
3. 静态链接库和动态链接库的区别

**运行时环境**：运行在用户态，它们也会使用系统调用。
**静态链接库**	\*.OBJ文件的简单合集。
**部分链接** 
	1. 一种静态链接库技术
	2. 将静态链接库中的*.OBJ进行预链接，并	且除去（或混淆）所有的内部引用符号。
	3. 符号除去：
		1. 将所有的库代码编译成一整个二进制映像，
		2. 其中代码和数据都使用 **位置无关方式**，
		3. 并且除去内部符号
	4. 符号混淆：将所有的内部符号以及对其的引用全部修改为无意义的随机字符串。

**多道程序共存**
1. 简单分区
	- 将物理内存切割成几个块，一个块运行一个应用程序，或者放置一个应用程序的某个段。
	- 各个应用程序的各个段在链接时就决定好要放置在什么地方。
	- 优点：
		- 配置简单：不需要额外的硬件就可以实现
		- 权限控制简单：针对每一次内存访问进行合法性检查，就可以防止访问越界
	- **内存保护单元**
		1. 常用于简单分区布局的存储器权限控制工具。内存保护单元使 Memory 用一个内存保护表，表中每一项都包括 **区间范围** 和 **区间权限** 一对信息。由应用程序发起的每一次内存访问都需要经过内存 Unit 保护表指定的权限检查：
			1.  访问的地址必须落在某分区的地址范围之中。
			2. 发起的访问的性质（读，写，执行）必须是该分区的权限允许的。
2. 虚拟地址空间
	1. **内存管理单元**：一种能依照某种规则将对**逻辑地址**的访问**转换**成对对应的**物理地址**访问的硬件。MMU
3. 分段：按段划分虚拟地址
	- 段式内存管理单元 S-MMU
4. 分页：按页划分虚拟地址
	- 页式内存管理单元 P-MMU

**静态链接库问题**：
1. **重复存储**：库会被在每一个可执行文件中保留一个副本，浪费磁盘。
2. **更新困难**：每次更新库之后，都需要把用到它的可执行文件重新链接。
3. **重复加载**：库会被在每一个虚拟地址空间中保留一个副本，进而导致在物理地址空间中也产生重复的副本，浪费内存，也浪费载入时间。

**动态链接库**
- 在可执行程序被加载时，作为独立文件单独加载的库文件。**它不包含在可执行文件之内**；可执行文件将在需要它们的时候加载它们。
- **动态库重定位**：
	- 不管这个库重定位到哪里，多个应用程序都必须以同样的虚拟地址引用这个库。
	- （会变的是物理地址中代码的重定位信息，而物理地址中库文件只有一份，所以所有虚拟地址中库文件位置要相同）
- **位置无关代码**：
	- 加载到任何绝对地址都可以直接运行的代码。其所有的地址访问（包括代码和变量）都相对于某指针，常见的是IP、SP或BP。
	- （和**动态库重定位**一样针对的是库文件内容而不是调用它的程序的内容）
- 共享库的数据段：**除了.rodata，其它无法共享**！
	- 本质上讲 .rodata 可以共享，就像 .text 一样
	- .rwdata .zidata .heap .stack 不可共享



---
## 指令流 PPT4.1
前瞻：
指令流的3个状态是什么？
指令流执行顺序有哪些？
在合作执行时，指令流让出 CPU 的两种方法

**顺序执行**
一个指令流执行完成后，再去执行另一个指令流。

**合作执行**：**自愿放弃**
指令流在不需要CPU的时候可以自愿放弃CPU。**自愿放弃CPU** 以及 **执行完毕** 是交还CPU控制权的唯二方法。

**并发执行**/**抢占式执行**

**上下文**
- 一个指令流的上下文就是足够使其恢复被打断时的状态的内容。
- 这一般包括了其 **寄存器组** 和 **执行栈**。在切换指令流时，一个非常方便的做法就是把寄存器组保存在执行栈上，合适时再恢复


![[Pasted_image_20251217152534.png]]

阻塞态：阻塞态通常是因为线程在等待某些资源（如I/O操作）而进入的状态（与时间片用完无关）


---
## 线程 PPT4.1、4.2

前瞻
线程的上下文和状态放在用户程序里面还是操作系统里面？


**线程** 是 **时间分配** 的基本对象
优先级一般用一个数值来代表，在绝大多数系统中，数字越 **小**，优先级越高。
Windows 的优先级数字越**大**，优先级越高

**线程控制块** TCB
**操作系统**（说明是在操作系统中，回答前瞻问题） 用以描述和管理线程的内核对象，一般至少包括线程的 **时间预算、优先级、运行状态及上下文**。


线程的上下文
线程上下文：和指令流上下文一样，线程的上下文也是其**寄存器组**。
线程的状态：和指令流状态一样：运行、就绪、阻塞

问题：线程的上下文和状态放在用户程序里面还是操作系统里面？
Linux：每个线程独立的内核栈，将通用寄存器上下文保存在内核栈里面（方法2）
现在有两种方法：
方法1：使用一个内核栈，切换线程时将上下文拷贝保存到 TCB 中
方法2：每个线程有独立的内核栈，上下文保存在内核栈中。
两者区别：？


### 处理器调度算法
时间片轮转法 RR：RR 是抢占式的，因为它会打断超时线程的执行。



---

## 进程 PPT5.1

前瞻：
1. 进程是什么？

### 程序内的分配
**分配**
- 应用程序的某个**指令流发出**一个从堆中申请内存的**请求**。
- 算法将从**堆**切出一部分内存并返回给这个请求。
- C语言的 **malloc、calloc** 等函数会从堆中申请内存。
**释放**	
- 指令流不再使用这个内存块，并将其归还回堆中供以后申请。
- C语言的 **free** 函数将会释放内存，将内存归还给堆。

**离线分配法**
- 用最小的堆满足某个分配

**在线算法**
- **竞争比**：
![[Pasted_image_20251218165337.png]]



**进程**
- 操作系统提供给应用程序的一种对地址空间的抽象机制。它是 **内存分配** 的基本对象。
- 而线程是 **时间分配** 的基本对象
- 进程与线程：线程通过在进程里运行，将CPU时间转化为对地址空间（以及其它资源）的一个访问操作次序，从而实现应用程序的功能。




应用程序视角：在应用程序看来，自己的逻辑组织是一系列段。
操作系统视角：在操作系统看来，应用程序的空间组织是一个或一系列进程。





PPT5.1 **Belady异常**：在某些资源分配机制下，增加资源反而会造成性能下降和效率降低的现象。
- 出现原因：没有 **栈式性质**，或称 **包含性性质**

### 页面置换的其它考量

**运行是最低目标**
- 操作系统往往会给多得多的页，来覆盖程序的工作集，保证程序能高效运行。

**抖动**（Thrashing）
- 当被分配的页数小于当前工作集的时候，缺页率会大幅增长。
- 工作集有短期、中期和长期三个层面，抖动也是这样，存在着短期抖动、中期抖动和长期抖动。

**分配策略的动态性**
- 一个进程的页数量可以是静态决定的也可以是动态调整的。
- 如果工作集的大小已知，只要将页数量设置为那个固定值就好了；
- 如果出现阶段性的需求，就需要动态决定进程需要多少页框。

**置换策略的全局性**
- 页置换可以仅仅在一个进程内部发生，也可以在进程间发生。
- 前者配合静态分配可以将一个进程引发的抖动限制在自身之内，但页利用效率低；
- 后者配合动态分配则正好相反，可能使一个进程的抖动影响其它进程。





操作系统并不知道指令流的存在。

---

### 进程的实现PPT5.2
进程控制块 Process Control Block  PCB
- 至少包含进程 Process 的**地址空间描述符表**及一些**其他权限表**
- 身份信息、统计信息、线程信息
- - 总是位于内核空间
- 与 TCB 不同，没有内核模式的 CPU 不需要也无法（用硬件手段）实现PCB


---

## 设备管理PPT7

软件：
硬件：

高速设备：数据传输速率高于或接近CPU处理能力的设备。
低速设备：数据传输速率低于CPU处理能力的设备。

块设备：一般是外存，其 I/O 需要被缓冲
字符设备：鼠标键盘打印机等，不需要缓冲，
其它设备：网卡等，其I/O需要非常复杂的缓冲方式。

假脱机设备：多个任务无法真正同时占用该设备，但由于该设备具备请求队列，因此可将多个任务的请求并发提交到该队列，设备将以较长的等待间隔依次响应这些请求。

鼠标键盘数据传输方式：中断（最好）
键盘扫描码：分按下键和释放键

---

**时钟源**：时间基准
**晶体振荡器**：Crystal
- 简称晶振
- 以石英晶体为核心的一种频率基准器件
**定时器** Timer
- 带可编程计数上限的计数器。
- 一组D触发器负责计数，另一组 Timer 触发器则储存一个上限值。
- 一个比较器负责比较这两组值，一旦计数值达到上限值（溢出），就将计数值重置为0。
**定时器中断**
- 一旦定时器的输入时钟频率已知，那么其溢出的时间间隔（时间片）也就决定了。我们可以设置定时器在每次溢出时产生一个中断报告给操作系统，操作系统就能够强制定期启动调度器。

**实时时钟** Real-Time Clock  RTC 
- 电子万年历
**实时时钟中断**
- 和定时器中断不同，实时时钟中断仅在特殊场合使用。
- 实时时钟可以设置闹钟，一旦闹钟时间点到，便允许向操作系统发送一个中断。该中断除了做日常事件提醒外还可以触发主板定时开关机。

系统启动时，从实时时钟读取当前时间，然后系统便自行维护时间的走动。
系统中的一切行为都以系统时间为基准。
问题一	随着系统的运行，系统时间和实时时钟时间产生差异怎么办？
以实时时钟为准。

**系统时间**
- 不是什么定时器

**软定时器**
- 用系统定时器作为时基来模拟一系列的软件定时器。

---

## 块设备（外存）PPT7

**硬盘的接口** 
机械硬盘等外存的接口是文件系统，提供按文件名存取文件内容的功能。




---

## 条件变量
1. 阻塞（并入队）	Wait（and Queue）
	- 当条件不满足时调用的原子操作。
	- 需要传入当前指令流所持有的锁；
	- 当前指令流将阻塞，将自己加入等待队列，并等待唤醒。
	- 这个操作总是释放当前指令流持有的锁，令其它指令流获得进入临界区的机会。

2. 唤醒（并出队）	Signal/Wakeup（and Dequeue）
	- 当判断条件可能满足时使用的原子操作。
	- 它将从等待队列头部唤醒一个指令流，并使被唤醒的指令流重新获得锁。**即让唤醒的指令流阻塞在锁上**
	- 如果当前没有指令流在等待（也即等待队列为空），此次唤醒将丢失。

3. 唤醒全部	Signal/Wakeup All
	- 唤醒等待队列中所有的指令流，并使其中随机一个获得锁。
	- 其它的无法获得锁的指令流继续因为得不到锁而阻塞。此时这些阻塞的指令流并非阻塞在条件变量上，而是阻塞在锁上；
	- 一旦那个当前持有锁的指令流释放锁，那些阻塞在锁上的指令流中的其中一个就会立即获取锁而继续执行，直到所有的指令流都获取过一遍锁。


--- 
## 
### 死锁条件
**互斥条件**	系统中存在有一定互斥性的资源。
**持有条件**	指令流已获得的资源不释放，且新请求不撤销。
**循环等待**	指令流互相循环等待资源释放，不能进展。

其中，持有条件又可以细分为如下两条：
**保持请求**	指令流不主动放弃已经持有的资源。
**无法剥夺**	不允许指令流互相剥夺资源；非抢占。




---

## 问题

1. \_\_\_\_\_\_\_（类型的）操作系统中，应用程序必须请求守护进程中的策略分配敏感资源，守护进程则互相通信或转而使用内核提供的机制完成这些分配操作。PPT2
2. 能对敏感资源进行操作、仅能在内核模式下执行的指令叫 **特权指令**。PPT2P66
3. 为何要给 INT3 分配特殊的单字节编码?

--- 
## 要背

**调度算法**
1. FCFS 先来先服务
2. SJF 短作业优先
3. FP 固定优先级
4. RR 时间片轮转
5. FPRR 
6. HRRN 相应比高优先(Highest Response Ratio Next)


文件
**属性**：
1. 文件名
2. 文件大小、创建日期、修改日期、文件权限

**逻辑结构**（程序员看到的逻辑上的文件组织形式）
1. 字节流结构（最基本的结构）
2. 记录结构
3. 索引结构

**文件的存储**：
1. 连续分配法
2. 链接分配法（每个块内部放置一个指针，指向下一个块）
3. 扩展分配法
4. 索引分配法



### 名词
**文件系统**
- 将文件中的文件块按照一定的方法最终映射到物理设备上的物理块，并在物理存储器特性的限制下提供尽可能高的信息管理效率。提供这种功能的软件栈称为文件系统。



