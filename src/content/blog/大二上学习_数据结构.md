---
title: '数据结构'
description: 'study notes'
publishDate: 2025-11-02
category: course
tags: []
---

[toc]

## 扩展
### c++报错
```cpp
struct MatrixTerm{
    MatrixTerm(int x，int y,int v):row(x),col(y),value(v){}
    int row,col;
    T value;
};
terms=new MatrixTerm<T>[rows*cols];//报错原因：没有默认构造函数
```
```cpp
class ...{
    public:
        sparseMatrix():rows(0),cols(0),Size(0){}//class类构造函数
    private:
        int rows,cols;
        MatrixTerm<T>* terms;
        int Size;
}
delete[] terms;//报错原因：terms没有初始化，应该加上terms(NULL)
```
### 直接将terms[x.Size++]变成我想要的元素，而不分配额外空间
错：`terms[Size++]=new MatrixTerm(i,j,tem);`

对：`terms[Size++]=MatrixTerm(i,j,tem);`

### 模板类里实现特化
```cpp
template <>
int chain<int>::deal(){
    int j=0;
    int ans=0;
    for(iterator i=begin();i!=end();++i){
        ans+=(i->val*j++);
    }
    return ans;
}
```
### iterator的实现
从array和chain类中定义iterator里的typedef由来
```cpp
#include <iostream>
#include <iterator>
#include <vector>

template<typename T>
class MyBidirectionalIterator {
public:
    using iterator_category = std::bidirectional_iterator_tag; // 迭代器类别
    using value_type = T;
    using difference_type = std::ptrdiff_t;
    using pointer = T*;
    using reference = T&;
    MyBidirectionalIterator(pointer ptr) : _ptr(ptr) {}
    reference operator*() const {
        return *_ptr;
    }
    // 前缀递增
    MyBidirectionalIterator& operator++() {
        ++_ptr;
        return *this;
    }
    // 后缀递增
    MyBidirectionalIterator operator++(int) {
        MyBidirectionalIterator tmp = *this;
        ++_ptr;
        return tmp;
    }
    // 前缀递减
    MyBidirectionalIterator& operator--() {
        --_ptr;
        return *this;
    }
    // 后缀递减
    MyBidirectionalIterator operator--(int) {
        MyBidirectionalIterator tmp = *this;
        --_ptr;
        return tmp;
    }
    // 比较运算符
    bool operator==(const MyBidirectionalIterator& other) const {
        return _ptr == other._ptr;
    }
    bool operator!=(const MyBidirectionalIterator& other) const {
        return _ptr != other._ptr;
    }
private:
    pointer _ptr; // 指向元素的指针
};
int main() {
    std::vector<int> vec = {1, 2, 3, 4, 5};
    MyBidirectionalIterator<int> it(vec.data());
    // 正向遍历
    for (size_t i = 0; i < vec.size(); ++i, ++it) {
        std::cout << *it << " "; // 输出: 1 2 3 4 5
    }
    std::cout << std::endl;
    // 反向遍历
    for (size_t i = 0; i < vec.size(); --it) {
        std::cout << *it << " "; // 输出: 5 4 3 2 1
    }
    return 0;
}
```